import 'package:flutter/material.dart';
import 'package:get/get.dart';
void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});
  @override
  Widget build(BuildContext context) {
    return GetMaterialApp(
      title: 'Flutter Demo',
      debugShowCheckedModeBanner: false,
      theme: ThemeData(
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.deepPurple),

        useMaterial3: true,
      ),
      home:LandingPage(),
    );
  }
}

class LandingPage extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    double height = Get.height;
    double width = Get.width;
    GlobalKey<ScaffoldState> onclick = GlobalKey<ScaffoldState>();
    return Scaffold(
      key: onclick,
      appBar: (width < 1000)?AppBar(
        backgroundColor: Colors.indigo,
        centerTitle: true,
        leading: IconButton(onPressed: (){
          onclick.currentState?.openDrawer();
        }, icon: Icon(Icons.menu,color: Colors.white,)),
        title: Center(
          child: Row(
            mainAxisAlignment: MainAxisAlignment.center,
            crossAxisAlignment: CrossAxisAlignment.center,
            children: [
              Icon(Icons.location_on_outlined,color: Colors.white,),
              Text("houzez",style: TextStyle(color: Colors.white,fontWeight: FontWeight.bold),),
            ],
          ),
        ),
        actions: [
          IconButton(onPressed: (){

          }, icon: Icon(Icons.account_circle_outlined,color: Colors.white,))
        ],
      ):null,
      drawer: Drawer(
        child: ListView(
          padding: EdgeInsets.zero,
          children: <Widget>[
            ListTile(
              title: Text('HOME',style: TextStyle(fontWeight: FontWeight.bold,fontSize: 15,color: Colors.blue),),
              trailing: Icon(Icons.expand_more_outlined),
              onTap: () {
                Navigator.pop(context);
              },
            ),Divider(),
            ListTile(
              title: Text('PROPERTIES',style: TextStyle(fontWeight: FontWeight.bold,fontSize: 15,color: Colors.blue),),
              trailing: Icon(Icons.expand_more_outlined),
              onTap: () {
                // Handle navigation to Settings
                Navigator.pop(context);
              },
            ),Divider(),
            ListTile(
              title: Text('PROPERTY',style: TextStyle(fontWeight: FontWeight.bold,fontSize: 15,color: Colors.blue),),
              trailing: Icon(Icons.expand_more_outlined),
              onTap: () {
                // Handle Logout action
                Navigator.pop(context);
              },
            ),Divider(),
            ListTile(
              title: Text('RETAILER',style: TextStyle(fontWeight: FontWeight.bold,fontSize: 15,color: Colors.blue)),
              trailing: Icon(Icons.expand_more_outlined),
              onTap: () {
                // Handle Logout action
                Navigator.pop(context);
              },
            ),Divider(),
            ListTile(
              title: Text('OTHERS',style: TextStyle(fontWeight: FontWeight.bold,fontSize: 15,color: Colors.blue)),
              trailing: Icon(Icons.expand_more_outlined),
              onTap: () {
                // Handle Logout action
                Navigator.pop(context);
              },
            ),
          ],
        ),
      ),
      body: SingleChildScrollView(
        child: Padding(padding: EdgeInsets.zero,child: Column(
          children: [
            Stack(
              fit: StackFit.loose,
              children:[
                Container(
                width: width,
                height: height*0.8,
                
                child:Image(image: AssetImage("assets/images.jpg"),fit: BoxFit.cover,width:width,height:height*0.8,colorBlendMode: BlendMode.luminosity,color: Colors.blue.withOpacity(0.8),)
              ),
                Positioned(top: 50,left:20,right:20,child: Text("Welcome To Houzez",textAlign: TextAlign.center,softWrap: true,style: TextStyle(color: Colors.white ,fontSize: 24))),

                Positioned(top:150,left:20,right:20,child: Text("Houzez is an innovative real estate WordPress theme that helps to ensure your website’s success in this super-competitive market.",textAlign:TextAlign.center,softWrap: true,style: TextStyle(color: Colors.white,fontSize: 15),))
            

              ])
          ],
        ),),
      ),
    );

  }
}
